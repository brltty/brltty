# This is a sample configuration file for the BRLTTY application. 
# BRLTTY expects to find its configuration file in "/etc/brltty.conf". 
# If it doesn't exist, then BRLTTY silently continues, but may require
# that certain command line options be explicitly supplied.

# Blank lines are ignored. The character '#', anywhere on a line,
# initiates a comment; all characters from it to the end of that line
# are ignored.

# Each configuration entry consists of a keyword followed by its operand.
# An arbitrary amount of white space, (blanks and/or tabs), may occur
# before the keyword, as well as before and after the operand. Keyword
# processing is not case sensitive. Examples of valid entries are:
#
#      Braille-Driver pm # Papenmeier braille displays.
#      braille-device /dev/ttyS0 # The first Linux serial port.
#      SPEECH-DRIVER  fv # The Festival Text to Speech System.

# The default settings given within the following descriptions assume no
# special build options (see the configure script in the top-level
# directory of the source tree.


############################
# Generic Braille Settings #
############################

# The braille-driver directive specifies the dynamically loadable library
# which implements the driver for the braille display. It can be one of:
# - The absolute path to the library (e.g. /lib/brltty/libbrlttybal.so).
# - The name of the library (e.g. libbrlttybbn.so). The operating system 
#     will search for it (see the man page for dlopen(3)).
# - The two-letter code of any braille display driver supplied with BRLTTY.
# If not specified, no braille support will be available.
# (can be overridden with the -b [--braille-driver=] option)
#braille-driver	al	# Alva
#braille-driver	bl	# BrailleLite
#braille-driver	bn	# BrailleNote
#braille-driver	cb	# CombiBraille
#braille-driver	ec	# EcoBraille
#braille-driver	eu	# EuroBraille
#braille-driver	ht	# HandyTech
#braille-driver	lt	# LogText
#braille-driver	mb	# MultiBraille
#braille-driver	md	# MDV (protocol 5)
#braille-driver	mn	# MiniBraille
#braille-driver	pm	# Papenmeier
#braille-driver	ts	# TSI
#braille-driver	va	# Vario/RBT (emulation 1)
#braille-driver	vd	# VideoBraille
#braille-driver	vh	# Vario/RBT (emulation 2)
#braille-driver	vo	# Voyager
#braille-driver	vs	# VisioBraille

# The braille-device directive specifies the port to which the braille
# display is connected. Relative paths are anchored at "/dev". If not
# specified, the first serial port will be used.
# (can be overridden with the -d [--braille-device=] option)
#braille-device	ttyS0	# Primary Linux serial port (old-style).
#braille-device	tts/0	# Primary Linux serial port (devfs).
#braille-device	ttya	# Primary Solaris serial port.
#braille-device	tty0p0	# Primary HP-UX serial port.
#braille-device	tty00	# Primary OpenBSD serial port.
#braille-device	ttyUSB0	# Primary Linux USB serial converter (old-style).
#braille-device	usb/tts/0	# Primary Linux USB serial converter (devfs).
#braille-device	brlvger	# Primary Linux USB Voyager port (old-style).
#braille-device	brlvger0	# Primary Linux USB Voyager port (devfs 2.4).
#braille-device	usb/brlvger0	# Primary Linux USB Voyager port (devfs 2.6).

# The text-table directive specifies which text translation table to
# use. Relative paths are anchored at "/etc/brltty". If not specified,
# "nabcc" will be used.
# (can be overridden with the -t [--text-table=] option)
#text-table	cz	# Czech (iso-8859-2)
#text-table	da	# Danish
#text-table	de	# German
#text-table	es	# Spanish
#text-table	fr	# French
#text-table	fr-cbifs	# French (Code Braille Informatique Français Standard)
#text-table	it	# Italian
#text-table	nabcc	# North American Braille Computer Code
#text-table	no-h	# Norwegian and German
#text-table	no-p	# Norwegian
#text-table	pl	# Polish (iso-8859-2)
#text-table	ru	# Russian (koi8-r)
#text-table	se1	# Swedish
#text-table	se2	# Swedish
#text-table	uk	# United Kingdom English
#text-table	us	# American English
#text-table	vni	# Vietnamese

# The attributes-table directive specifies which attributes translation
# table to use. Relative paths are anchored at "/etc/brltty". If not
# specified, "attributes.tbl" will be used.
# (can be overridden with the -a [--attributes-table=] option)
#attributes-table	attributes.tbl	#
#attributes-table	attrib.tbl	#

# The contraction-table directive specifies which braille contraction
# table to use. Relative paths are anchored at "/etc/brltty". If not
# specified, no contraction table will be available.
# (can be overridden with the -c [--contraction-table=] option)
#contraction-table	compress.ctb	# Remove excess white space.
#contraction-table	en-us-g2.ctb	# Grade 2 American English


#############################
# Braille Driver Parameters #
#############################

# The braille-parameters directive passes non-generic parameters through
# to the braille driver.
# (can be overridden with the -B [--braille-parameters=] option)
#braille-parameters parameter=setting,...

# BrailleLite Braille Driver Parameters
#braille-parameters BaudRate=9600 # [300,600,1200,2400,4800,9600,19200,38400]
#braille-parameters KbEmu=yes # [yes,no]

# BrailleNote Braille Driver Parameters
#braille-parameters StatusCells=0

# Papenmeier Braille Driver Parameters
#braille-parameters ConfigFile=/etc/brltty/brltty-pm.conf
#braille-parameters DebugKeys=no # [no,yes]
#braille-parameters DebugReads=no # [no,yes]
#braille-parameters DebugWrites=no # [no,yes]

# Virtual Braille Driver Parameters
#braille-parameters Mode=client # [client,server]
#braille-parameters Socket=localhost:35752 # [/path/to/socket, host:port]

# Voyager Braille Driver Parameters
#braille-parameters Repeat_Init_Delay=120 # [milliseconds]
#braille-parameters Repeat_Inter_Delay=60 # [milliseconds]
#braille-parameters Dots_Repeat_Init_Delay=500 # [milliseconds]
#braille-parameters Dots_Repeat_Inter_Delay=60 # [milliseconds]


###########################
# Generic Speech Settings #
###########################

# The speech-driver directive specifies the dynamically loadable library
# which implements the driver for the speech synthesizer. It can be one of:
# - The absolute path to the library (e.g. /lib/brltty/libbrlttysgs.so).
# - The name of the library (e.g. libbrlttyscb.so). The operating system 
#     will search for it (see the man page for dlopen(3)).
# - The two-letter code of any speech synthesizer driver supplied with BRLTTY.
# If not specified, no speech support will be available.
# (can be overridden with the -s [--speech-driver=] option)
#speech-driver	al	# Alva
#speech-driver	bl	# BrailleLite
#speech-driver	cb	# CombiBraille
#speech-driver	es	# ExternalSpeech
#speech-driver	fv	# Festival Text to Speech Package
#speech-driver	gs	# GenericSay (pipes to /usr/local/bin/say)
#speech-driver	tv	# Televox
#speech-driver	vv	# ViaVoice Text to Speech System


############################
# Speech Driver Parameters #
############################

# The speech-parameters directive passes non-generic parameters through
# to the speech driver.
# (can be overridden with the -S [--speech-parameters=] option)
#speech-parameters parameter=setting,...

# ExternalSpeech Speech Driver Parameters
#speech-parameters Program=/usr/local/bin/externalspeech
#speech-parameters Uid=65534
#speech-parameters Gid=65534

# GenericSay Speech Driver Parameters
#speech-parameters Command=/usr/local/bin/say

# ViaVoice Speech Driver Parameters
#speech-parameters IniFile=/usr/lib/ViaVoiceTTS/eci.ini
#speech-parameters SampleRate= # [8000,11025,22050]
#speech-parameters AbbreviationMode= # [on,off]
#speech-parameters NumberMode= # [word,year]
#speech-parameters SynthMode= # [sentence,none]
#speech-parameters TextMode= # [talk,spell,literal,phonetic]
#speech-parameters Language= # [AmericanEnglish,BritishEnglish,CastilianSpanish,MexicanSpanish,StandardFrench,CanadianFrench,StandardGerman,StandardItalian,SimplifiedChinese,BrazilianPortuguese]
#speech-parameters Voice= # [AdultMale,AdultFemale,Child,ElderlyFemale,ElderlyMale]
#speech-parameters VocalTract= # [male,female]
#speech-parameters Breathiness= # [0-100]
#speech-parameters HeadSize= # [0-100]
#speech-parameters PitchBaseline= # [0-100]
#speech-parameters PitchFluctuation= # [0-100]
#speech-parameters Roughness= # [0-100]
#speech-parameters Speed= # [0-250]
#speech-parameters Volume= # [0-100]


##########################
# Miscellaneous Settings #
##########################

# The preferences-file directive specifies the file which is used for
# saving and restoring the preferred default settings. Relative paths
# are anchored at "/etc/brltty". If not specified, "brltty-XX.prefs"
# (where "XX" is the two-letter identifier of the braille driver) will
# be used. 
# (can be overridden with the -p [--preferences-file=] option)
#preferences-file /etc/brltty.prefs

# The library-directory directive specifies the absolute path to the
# directory which contains the dynamically loadable drivers. If not
# specified, "/lib/brltty" will be used.
# (can be overridden with the -L [--library-directory=] option)
#library-directory /lib/brltty

# The data-directory directive specifies the absolute path to the directory
# which contains driver data files (help files, configuration files, etc.). If
# not specified, "/etc/brltty" will be used.
# (can be overridden with the -D [--data-directory=] option)
#data-directory /etc/brltty

# The tables-directory directive specifies the absolute path to the directory
# which contains the text and attributes tables. If not specified,
# "/etc/brltty" will be used.
# (can be overridden with the -T [--tables-directory=] option)
#tables-directory /etc/brltty

# The contractions-directory directive specifies the absolute path to the
# directory which contains the contraction tables. If not specified,
# "/etc/brltty" will be used.
# (can be overridden with the -C [--contractions-directory=] option)
#contractions-directory /etc/brltty

# The api-parameters directive passes parameters to the Application
# Programming Interface.
# (can be overridden with the -A [--api-parameters=] option)
#api-parameters parameter=setting,...
#api-parameters host=
#api-parameters port=35751
#api-parameters keyfile=/etc/brlapi.key


############################
# Screen Driver Parameters #
############################

# The screen-parameters directive passes non-generic parameters through
# to the screen driver.
# (can be overridden with the -X [--screen-parameters=] option)
#screen-parameters parameter=setting,...

# Linux Screen Driver Parameters
#screen-parameters Acm=auto # [auto,iso01,vt100,cp437,user]
#screen-parameters DebugAcm=no # [no,yes]
#screen-parameters DebugSfm=no # [no,yes]
#screen-parameters DebugCtt=no # [no,yes]


