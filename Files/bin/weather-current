#!/bin/bash
. brltty-prologue.bash

addProgramOption m flag monitor "monitor the weather"
parseProgramArguments "${@}"

set -- /brltty/current/libexec/owm -L -U -- weather
"${monitor}" && set /brltty/current/libexec/run-command -c -i 3600 -d 1800 -- "${@}"
"${@}"
exit "${?}"
