#!/bin/bash

installTree="/brltty/current"
. "${installTree}/bin/brltty-prologue.sh"

addTestModeOption
parseProgramArguments "${@}"

executeHostCommand exec screen -m -c "${installTree}/etc/canute-screenrc"
exit 0
